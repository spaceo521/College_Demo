#Model-2 Now
y=iris[['5.1']]
#These double brackets without anything written inside the outermost bracket as the resultant should also be in the form a dataframe.

#Yaha par multiple linear regression implement karr rahe hai  
x=iris[['1.4','0.2','3.5']]

lr2= LinearRegression()

lr2.fit(x_train,y_train)

y_pred=lr2.predict(x_test)

mean_squared_error(y_test,y_pred)

import datetime as dt

#Converting datetime to ordinal.
maharashtra['date']=maharashtar['date'].map(dt.datetime.tooridnal)

from sklearn.ensemble import RandomForestRegressor

dtr= RandomForestRegressor()


#Logistic Regression.
#Its is graph is like a s unlike the linear regression which is a straight line.
#Its basically used for classification. For example: the example of virat kohli runs and winnign the match in the video
#which is 0-lose and 1- win.
#This s-curve is called the sigmoid curve.
from sklearn.linear_model import LogisticRegression

l3=LogisticRegression()



    import numpy as np
from matplotlib import pyplot as plt

ys = 200 + np.random.randn(100)
x = [x for x in range(len(ys))]

plt.plot(x, ys, '-')
plt.fill_between(x, ys, 195, where=(ys > 195), facecolor='g', alpha=0.6)

plt.title("Sample Visualization")
plt.show()
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.3)
